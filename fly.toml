app = "board-worker"
primary_region = "bom"

[build]
  dockerfile = "Dockerfile.worker"

[env]
  WORKER_ID = "fly-prod-1"
  NODE_ENV = "production"

[experimental]
  auto_stop_machines = false   # always on
  auto_start_machines = true

# No http service required; this is a background worker
