FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
# Install with dev deps so we can run TS directly with tsx
RUN npm ci
COPY . .
# Run the TypeScript worker via tsx runtime
CMD ["npx", "tsx", "workers/resume-queue-worker.ts"]
